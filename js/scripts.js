$(function() {
  $(".needCarousel, .offerCarousel").flickity({
    // options
    cellAlign: 'left',
    contain: true,
    freeScroll: true,
    prevNextButtons: false,
    pageDots: false,
    autoPlay: true
  });
});
